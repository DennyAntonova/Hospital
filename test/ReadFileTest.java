import com.company.Appointment;
import com.company.Patient;
import com.company.ReadFile;
import com.company.SortingReversedHoursForDoctor;
import org.junit.Assert;
import org.junit.Test;

import java.util.ArrayList;

import static com.company.ReadFile.readAppointmentsFromCSV;

public class ReadFileTest {
  /*  Patient patient = new Patient(1, "Stefan", "Ivanov", 85);

    @Test
    public void testCreatePatient(){
        Patient expected=  new Patient(1, "Stefan", "Ivanov", 85);
        Patient actual= (Patient) ReadFile.createPatient(patient);
        Assert.assertEquals(expected,actual);
    }
*/
}
